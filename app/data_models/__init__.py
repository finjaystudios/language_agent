"""Internal data models shared across services and modules.

These models define the structured objects passed around inside the
application, making the codebase easier to read, reason about, and maintain.
"""
